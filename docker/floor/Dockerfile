# Minimal image: there is essentially nothing to build, so a timing of this
# target is the fixed overhead of the pipeline around the build itself.
FROM alpine:3.21
RUN echo "floor" > /floor.txt
CMD ["sh", "-c", "echo floor && sleep infinity"]

# cold run 1

# cold run 2

# cold run 3
