ci: derive a slash-free artifact name from the target

What:
Compute a sanitised slug for the build target and use it in the artifact name.

Why:
Targets are paths, so a nested one such as `mono/svc-1` produced an artifact
name containing a forward slash, which the upload rejects. The builds
themselves succeeded; only the upload failed, which lost the timings the run
existed to collect.
